apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: three-tier
spec: 
  selector:
    role: api
  ports:
  - port: 3500
    targetPort: 3500   # 👈 MUST add this
    protocol: TCP
  type: ClusterIP